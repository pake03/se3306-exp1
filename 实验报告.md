# SE3306 实验一：CSR、SSR、SSG 渲染对比

本项目用相同的 10 篇 Web 渲染技术文章，对比三种页面生成方式：

- `lab1-csr`：浏览器执行 JavaScript 后生成文章列表。
- `lab1-ssr`：Node.js/Express 在每次请求时返回完整 HTML。
- `lab1-ssg`：构建阶段生成完整静态 HTML，访问时直接分发文件。

页面采用统一的技术博客视觉、响应式布局和 WebP 流程图，确保比较重点是 HTML 生成时机，而不是内容或样式差异。

## 运行项目

环境要求：Node.js 20 或更高版本。本次自检使用 Node.js 24 和 Microsoft Edge。

```powershell
npm.cmd install --cache .npm-cache
npm.cmd run build
npm.cmd run verify
```

分别启动：

```powershell
npm.cmd run preview:csr    # http://127.0.0.1:5173
npm.cmd run start:ssr      # http://127.0.0.1:3000
npm.cmd run preview:ssg    # http://127.0.0.1:4173
```

SSR 健康检查地址：`http://127.0.0.1:3000/health`。

## 项目结构

```text
se3306-exp1/
├─ lab1-csr/                 # Vite + 原生 JavaScript
│  ├─ index.html              # 只有空的 #app 容器
│  └─ src/main.js             # 浏览器端生成 10 篇文章
├─ lab1-ssr/                 # Express 服务端渲染
│  └─ server.js               # 逐请求生成 HTML
├─ lab1-ssg/                 # 构建时静态生成
│  ├─ build-ssg.js
│  └─ dist/index.html
├─ shared/                   # 统一 CSS、数据和 HTML 模板
├─ scripts/                  # 图片生成、静态预览和自动化自检
├─ docs/screenshots/         # 实际浏览器截图证据
├─ docs/results.json         # 机器可读自检结果
├─ 自检报告.md
└─ AI 使用声明.md
```

## 渲染机制对比

| 模式 | HTML 生成时间 | 初始 HTML 是否含文章 | 主要特点 | 适合场景 |
| --- | --- | --- | --- | --- |
| CSR | 浏览器运行时 | 否 | 首次 HTML 小，交互灵活，依赖 JavaScript | 后台系统、强交互 SPA |
| SSR | 每次 HTTP 请求 | 是 | 首屏和 SEO 友好，服务器承担请求时渲染成本 | 新闻、电商、个性化首屏 |
| SSG | 发布前构建 | 是 | 访问快、缓存简单、服务器成本低 | 文档、博客、帮助中心 |

## 实际性能结果

以下数据来自自动化脚本在同一台电脑、Microsoft Edge、1440×900 视口下各运行 3 次后的中位数。FCP/LCP 来自浏览器 Performance API；“性能估分”是基于本地 FCP/LCP 的自检估分，不冒充 Lighthouse 官方分数。SEO 是对 `title`、description、`h1` 和 10 篇语义文章的页面信号检查。

| 模式 | 初始 HTML | FCP | LCP | 性能估分 | SEO 信号 |
| --- | ---: | ---: | ---: | ---: | ---: |
| CSR | 546 B | 80 ms | 92 ms | 93 | 100 |
| SSR | 6763 B | 84 ms | 84 ms | 93 | 100 |
| SSG | 6766 B | 96 ms | 96 ms | 92 | 100 |

HTML 大小反映的是响应体差异：CSR 初始响应只包含页面壳，SSR 和 SSG 初始响应已经包含 10 篇文章正文。三者在本地测试中均为快速页面，不能仅凭一次本地测试宣称线上性能结论。

## 实验问题回答

### 1. 为什么 SPA 的 SEO 通常较弱？

传统 SPA 首次返回的 HTML 往往只有根容器，正文要等 JavaScript 下载、执行和请求数据后才出现。若爬虫没有执行脚本、执行时间有限，或者页面依赖客户端状态，抓取到的内容就会不完整。SSR 和 SSG 直接把主要正文放进初始 HTML，因此更容易被搜索引擎读取。

### 2. SSG 与 SSR 的本质区别是什么？

二者都能生成包含正文的 HTML，关键区别是生成时机。SSG 在构建发布阶段生成，访问时直接返回已经存在的文件；SSR 在用户请求到达时生成，每次请求都可以读取最新数据或根据用户信息生成不同页面。SSG 更利于缓存，SSR 更适合动态内容。

### 3. Web 渲染技术的演进概述

早期网站主要由服务器返回完整 HTML，浏览器负责展示；随后 Ajax 和 SPA 把更多路由、状态与界面更新交给浏览器，交互能力增强，但首屏和 SEO 需要额外处理。SSR 重新承担首屏 HTML 生成，SSG 则把稳定页面提前生成并交给 CDN。现在的框架通常采用混合渲染：不同页面或组件可以分别选择 CSR、SSR、SSG、ISR 和流式渲染，让数据新鲜度、速度、成本和交互体验取得平衡。

### 4. 前三个任务使用了什么服务器，机制是什么？

CSR 使用 Vite 开发/预览服务器分发静态入口和 JavaScript，页面正文由浏览器生成；SSR 使用 Node.js Express 服务器，收到 `/` 请求后调用模板函数拼出完整 HTML，再通过 HTTP 返回；SSG 使用 Node.js 构建脚本生成 `dist/index.html`，预览阶段由静态 HTTP 服务器直接读取并返回这个文件。

### 5. 实时股票页面如何选择 CSR、SSR、SSG 或 ISR？

股票价格需要持续更新，不能只依赖一次构建，因此纯 SSG 不合适，ISR 也只能解决有限时间间隔内的更新。推荐“SSR 首屏 + CSR/WebSocket 实时更新”：SSR 快速返回股票列表、标题和初始价格，客户端连接实时数据通道并更新报价、图表和自选状态。若业务极度个性化且首屏内容不重要，也可以采用纯 CSR；本实验的综合推荐是混合方案。

### 6. SSR 为什么还需要 Hydration？没有它会怎样？

SSR 返回的 HTML 负责快速展示，但 HTML 本身不会自动拥有客户端事件、状态和路由能力。Hydration 会让客户端 JavaScript 复用已有 DOM，并将组件状态和事件监听器绑定回页面。如果没有 Hydration，页面可能看起来已经加载完成，但按钮、表单、切换和交互逻辑不会工作；如果直接重新渲染，还可能造成重复节点和额外性能开销。

## 截图索引

完整自检过程、每项状态和证据对应关系见 [自检报告.md](自检报告.md)。截图位于 `docs/screenshots/`，包括：

- 三种模式桌面端和移动端页面；
- CSR JavaScript 网络请求；
- 三种模式初始 HTML 源代码；
- SSR 两次请求时间变化、SSG 固定构建时间；
- 浏览器 Performance 三次测试中位数。

## 部署说明

部署配置已经准备好：`lab1-csr/vercel.json`、`lab1-ssg/vercel.json` 和根目录 `render.yaml`。由于当前环境没有进行 GitHub、Vercel 和 Render 的账号授权，本次不能创建真实线上项目、链接或线上截图，报告中将该项标记为“受阻”，没有伪造部署地址。

部署时建议：

1. 将整个 `se3306-exp1` 推送到 GitHub。
2. 在 Vercel 创建 CSR 项目，Root Directory 设为 `lab1-csr`，输出目录为 `dist`。
3. 在 Vercel 创建 SSG 项目，Root Directory 设为 `lab1-ssg`，构建命令为 `npm run build`，输出目录为 `dist`。
4. 在 Render 创建 Node Web Service，使用根目录 `render.yaml`，健康检查为 `/health`。

## 结论

本地实现和自动化验收均已完成。CSR、SSR、SSG 的源代码差异可以通过初始 HTML、Network 和动态/固定时间截图直接观察，适合提交实验报告；线上部署仍需在有账号授权的环境中完成。
